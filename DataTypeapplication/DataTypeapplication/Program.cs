using System;

namespace DataTypeapplication
{
    class Program
    {

        static void Main(string[] args)
        {
            Console.WriteLine("Size of int: {0}", sizeof(int)); // to get to know exact value of type or variable.
            // using sizeof() method.
            Console.WriteLine("Size of char: {0}", sizeof(char));
            // using sizeof() method.
            Console.WriteLine("Size of double: {0}", sizeof(double));
            // using sizeof() method.
            Console.WriteLine("Size of bool: {0}", sizeof(bool));
            // using sizeof() method.
          
            Console.ReadLine();
        }
    }
}